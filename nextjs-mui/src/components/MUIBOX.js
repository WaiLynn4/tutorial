import { Box } from "@mui/material";

export default function MUIBox(){
    return (
        <Box sx={{padding: 10  ,margin:5, backgroundColor: "grey.100"}}>
            This is a Box with padding and background.
        </Box>
    );
}